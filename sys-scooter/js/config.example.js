/**
 * Exemplo de configuração. Copie para config.js e preencha.
 * O arquivo config.js não é versionado no Git.
 *
 * - Uso OFFLINE (arquivo no PC): defina user e password para login local.
 * - Site no Vercel + API em outro servidor: defina apiBase com a URL da API (ex.: 'https://seusite.com.br/sys-scooter/api').
 */
window.SYS_SCOOTER_LOCAL_CREDENTIALS = {
  user: 'seu_login',
  password: 'sua_senha'
};
// URL da API quando o front está em outro domínio (ex.: Vercel). Ex.: 'https://www.assistenciaconsertcell.com.br/sys-scooter/api'
// window.SYS_SCOOTER_API_BASE = 'https://SEU_DOMINIO/sys-scooter/api';
