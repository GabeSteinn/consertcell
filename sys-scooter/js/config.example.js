/**
 * Exemplo de configuração para uso OFFLINE (arquivo local, sem servidor).
 * Copie este arquivo para config.js e preencha user e password.
 * O arquivo config.js não é versionado no Git (não será enviado ao GitHub).
 */
window.SYS_SCOOTER_LOCAL_CREDENTIALS = {
  user: 'seu_login',
  password: 'sua_senha'
};
